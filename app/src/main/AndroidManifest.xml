<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- miuix-blur declares minSdk 33 (runtime shaders). Blur use is guarded by
         isRuntimeShaderSupported() at runtime, so older API levels simply get opaque bars. -->
    <uses-sdk tools:overrideLibrary="top.yukonga.miuix.kmp.blur" />

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Walking simulation foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Android 16 Live Update eligibility (non-runtime; requested per notification) -->
    <uses-permission android:name="android.permission.POST_PROMOTED_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="PackageVisibilityPolicy,QueryAllPackagesPermission" />

    <application
        android:name=".manager.App"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:description="@string/description"
        android:label="@string/app_name"
        android:appCategory="productivity"
        android:hardwareAccelerated="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.XposedFakeLocation"
        tools:targetApi="31">
        <activity
            android:name=".manager.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.XposedFakeLocation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Off by default. The user opts in via Settings → External Control,
            which calls PackageManager.setComponentEnabledSetting(...) to flip
            this receiver to ENABLED at runtime. Until then, no external app
            (and no `adb shell am broadcast`) can reach it.
        -->
        <receiver
            android:name=".manager.control.ControlReceiver"
            android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="io.github.souitou.mockx.action.START" />
                <action android:name="io.github.souitou.mockx.action.STOP" />
                <action android:name="io.github.souitou.mockx.action.SET_LOCATION" />
            </intent-filter>
        </receiver>

        <!--
            Island stop button entry (模板13 按钮组件1). The Super Island system resolves the
            actionIntent URI against this receiver, so it must stay exported and always enabled.
        -->
        <receiver
            android:name=".manager.control.IslandStopReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="io.github.souitou.mockx.action.ISLAND_STOP_FIXED" />
            </intent-filter>
        </receiver>

        <!--
            Walk-simulation foreground service. Not exported: it is only commanded by the
            manager app's own UI via explicit intents. The `location` type keeps the per-second
            position tick alive while the user is in other apps or the screen is off.
        -->
        <service
            android:name=".manager.walking.WalkingSimulationService"
            android:exported="false"
            android:foregroundServiceType="location" />
        <service
            android:name=".manager.notification.FixedLocationNotificationService"
            android:exported="false"
            android:foregroundServiceType="location" />
    </application>
</manifest>
