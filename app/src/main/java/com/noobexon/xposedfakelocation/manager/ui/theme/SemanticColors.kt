package com.noobexon.xposedfakelocation.manager.ui.theme

import androidx.compose.ui.graphics.Color

// Semantic status accents shared across screens (success / warning). Error comes from
// MiuixTheme.colorScheme.error so it follows the active (incl. Monet) scheme.
val StatusSuccess = Color(0xFF34C759)
val StatusWarning = Color(0xFFFF9500)
