# 高德步行模拟功能问题追踪

> 项目：`/Volumes/SanDisk/Projects/XposedFakeLocation`
>
> 创建日期：2026-09-19
>
> 当前结论：**暂不通过验收**（维持）。代码层问题 P1-001/P1-002/P2-001 已于 2026-09-19 修复并有单元测试，
> P2-002 构建环境已修复并在当前环境完成全新验证；**P1-003 实体设备验收仍未执行**，完成后方可交付。

---

## P1-001：步行状态过期检测使用了错误的时间基准

- **状态**：已修复（2026-09-19，含单元测试，待设备复核）
- **修复记录**：新增纯 Kotlin `xposed/utils/WalkingStatePolicy.kt`（age 计算 + 过期判定：缺失→过期、负数→视为刚更新、超过 15s→过期）；
  `LocationUtil.tryApplyWalkingLocation` 改为统一使用 `System.currentTimeMillis()` 计算 age，不再与 `SystemClock.elapsedRealtime()` 相减。
  限流日志的时间基准保留 elapsedRealtime（仅进程内限流用途）。测试：`WalkingStatePolicyTest`（6 项，覆盖 15s 边界、负数、缺失）。
- **位置**：`app/src/main/java/com/noobexon/xposedfakelocation/xposed/utils/LocationUtil.kt:290-299`
- **关联位置**：`app/src/main/java/com/noobexon/xposedfakelocation/manager/walking/WalkingSimulationService.kt:214-222`

### 问题

Manager Service 写入 `walking_updated_at` 使用：

```kotlin
System.currentTimeMillis()
```

Xposed Hook 读取后使用：

```kotlin
SystemClock.elapsedRealtime() - updatedAt
```

前者是 Unix 墙上时间，后者是设备开机后的单调时间，二者不能相减。实际结果通常是巨大负数，因此 `WALKING` 状态几乎不会被判断为过期。

### 风险

前台 Service 被杀死、卡死或停止更新后，Xposed Hook 可能长期继续返回最后一个模拟坐标，导致目标应用收到过期位置。

### 修复要求

选择并统一一种时间方案：

- 推荐：`walking_updated_at` 统一使用 `System.currentTimeMillis()`，读取时也使用 `System.currentTimeMillis()`；
- 处理系统时间回拨：对负数 age 做安全处理；
- 或统一使用 `SystemClock.elapsedRealtime()`，但必须明确设备重启后持久化状态不能直接复用；
- 不要在同一个字段中混用两种时间基准。

建议验收逻辑：

```text
age = currentTimeMillis - walking_updated_at
age < 0       -> 按刚刚更新处理或判定异常
age <= 15000  -> 状态有效
age > 15000   -> WALKING 状态过期，不能继续返回旧动态坐标
```

### 验收标准

- 正常步行期间状态不会误判过期；
- 停止更新超过阈值后，Hook 不再继续返回旧动态位置；
- 设备重启、系统时间变化时不会出现永久有效状态；
- 为时间基准和过期边界增加单元测试。

---

## P1-002：停止/失败状态写入与 Service 销毁存在竞态

- **状态**：已修复（2026-09-19，代码结构保证时序，待设备证据）
- **修复记录**：新增 `finalizeSession(cleanup)`——终态写入运行在独立于 `serviceScope` 的
  协程上（`onDestroy` 取消不再影响清理），顺序为「终态写入完成 → stopForeground → 释放 WakeLock → stopSelf」
  （5s 超时防远程偏好写入挂起；`AtomicBoolean` 防重复进入）。
  同时引入 `stateWriteDispatcher`（单线程 FIFO）串行化全部共享状态写（阶段切换、tick 位置发布、到达写入），
  保证命令处理顺序即写入顺序。
  **世代隔离增补（2026-09-19 第二轮，规划 §5.1）**：新增 `KEY_WALKING_SESSION_ID`（UUID）——
  startWalkingSession 写入、stop/failed/READY 清除；tick/pause/resume/arrival 写入前经
  `WalkingSessionPolicy.mayWrite` 校验世代，被新一代取代的终态 finalizer（`isSuperseded`）
  不再触碰共享状态/通知/WakeLock/服务生命周期；同实例 STOP→START 后 `finalizing` 重新武装；
  RESUME 增加相位门（仅 WALKING/PAUSED 可恢复）。
  测试：`WalkingSessionPolicyTest`（7 项）、`WalkingCommandPolicyTest`（5 项）。
- **位置**：`app/src/main/java/com/noobexon/xposedfakelocation/manager/walking/WalkingSimulationService.kt:134-140`
- **关联位置**：同文件 `fail()` 方法附近

### 问题

当前停止流程类似：

```kotlin
serviceScope.launch { repository.stopWalkingSession() }
stopForeground(STOP_FOREGROUND_REMOVE)
stopSelf()
```

`stopSelf()` 触发 `onDestroy()` 后，`onDestroy()` 会取消 `serviceScope`。清理 Preference 的协程可能还没有执行，或者执行到一半就被取消。

`fail()` 使用异步写入后立即停止 Service，也存在同类问题。

### 风险

可能出现：

- 通知已经消失；
- Service 已经停止；
- 但 `walking_enabled` 仍为 true；
- `walking_phase` 仍为 WALKING；
- `is_playing` 仍为 true；
- 目标应用继续收到动态或旧的虚拟位置。

这是位置模拟功能的安全性和状态一致性问题，优先级为 P1。

### 修复要求

停止和失败流程必须保证：

```text
取消 tick
→ 完成共享 Preference 清理/失败状态写入
→ 停止前台通知
→ stopSelf
```

建议：

- 在 Service 自己的协程中顺序执行清理；
- 等待 `repository.stopWalkingSession()` 或 `markWalkingFailed()` 完成后再调用 `stopForeground()` 和 `stopSelf()`；
- 不要在写入完成前取消负责写入的 CoroutineScope；
- 必要时使用独立且受控的清理协程，并确保 Service 销毁不会取消关键提交；
- 停止流程应保持幂等。

### 验收标准

停止按钮返回后，读取共享状态必须满足：

```text
walking_enabled = false
walking_phase = IDLE
is_playing = false
walking_current_latitude/longitude 已清除或不可用
```

失败流程返回后必须满足：

```text
walking_enabled = false
walking_phase = FAILED
is_playing = false
walking_error_code = 对应错误码
```

并验证目标 App 不再收到旧动态坐标。

---

## P2-001：无 ticker 时收到暂停命令不会写入 PAUSED

- **状态**：已修复（2026-09-19，含单元测试）
- **修复记录**：新增纯 Kotlin `manager/walking/WalkingCommandPolicy.kt`——仅当共享状态为 `WALKING`
  时 PAUSE 产生 `PAUSED`，其余阶段（PAUSED/ARRIVED/IDLE/FAILED/READY/PLANNING/STOPPING）一律幂等 no-op；
  无 ticker 分支收养残留会话时现在会写入 PAUSED + 时间戳、保留当前位置并保持前台通知。
  START/PAUSE 近同时到达由串行状态写队列按命令顺序落盘（后到命令胜出，UI/通知/偏好/Hook 一致）。
  测试：`WalkingCommandPolicyTest`（3 项，覆盖全部阶段）。
- **位置**：`app/src/main/java/com/noobexon/xposedfakelocation/manager/walking/WalkingSimulationService.kt:108-115`

### 问题

当 `tickJob` 没有运行，但共享状态仍然是 `WALKING` 时，当前分支只执行：

```kotlin
startInForeground()
updateNotification()
return
```

没有调用 `repository.pauseWalkingSession()`。

### 风险

可能出现 UI 显示已暂停，但共享状态仍然是 WALKING，导致：

- Xposed Hook 继续按 WALKING 规则检查新鲜度；
- Service 重建后仍恢复行走；
- 通知操作和真实状态不一致。

### 修复要求

该分支也必须完成状态转换：

```text
确认路线有效
→ 写入 walking_phase = PAUSED
→ 更新时间戳
→ 保留当前位置
→ 启动/保持前台通知
```

如果共享状态已经不是 WALKING，则暂停命令应保持幂等，不应错误改变 ARRIVED、IDLE 或 FAILED 状态。

### 验收标准

覆盖以下时序：

1. Service 被系统重建但 ticker 尚未启动时点击暂停；
2. START 与 PAUSE 几乎同时到达；
3. 暂停后销毁并重建 Service；
4. 已暂停状态重复点击暂停。

所有情况下 UI、通知、共享 Preference 和 Xposed Hook 观察到的状态必须一致。

---

## P1-003：必须完成实体设备运行验收

- **状态**：未验证
- **范围**：全功能链路

### 当前证据不足

目前有历史构建产物和历史单元测试报告，但没有本轮可复核的实体设备运行证据。以下内容不能仅凭编译通过确认：

- 高德 Key 能成功请求步行路线；
- 高德路线点在地图上正确贴合道路；
- GCJ-02/WGS-84 转换没有明显偏移；
- 目标 App 每秒收到变化坐标；
- 暂停/继续/到达/停止行为正确；
- 切换 App 和锁屏后仍继续；
- 停止后真实位置恢复；
- Wi-Fi/基站定位不会把目标 App 拉回真实位置。

### 验收要求

使用已启用 Xposed/LSPosed 的实体 Android 设备，至少完成：

1. 设置页填写测试 Key；
2. 获取真实起点；
3. 地图选目标点；
4. 规划并显示步行路线；
5. 开始模拟；
6. 使用目标 App 观察坐标变化；
7. 暂停并确认坐标不变；
8. 继续并确认从暂停点恢复；
9. 切换到其他 App；
10. 锁屏等待一段时间；
11. 到达终点；
12. 停止并确认恢复真实位置；
13. 检查 Logcat、通知和共享状态；
14. 清除并重置测试 Key。

必须记录：

- Android 版本、ROM、设备型号；
- Xposed/LSPosed 版本；
- 目标 App 包名；
- 实际路线距离和模拟耗时；
- 关键状态转移；
- 失败时的脱敏日志。

不得记录或提交完整 API Key。

---

## P2-002：重新建立可复现的构建与测试环境

- **状态**：已解决（2026-09-19；同日第二轮复核刷新证据）
- **修复记录**：`gradle.properties` 中机器专属的 `org.gradle.java.home=C:/Users/Souit/...` 已删除，
  改为注释说明（用户级 `~/.gradle/gradle.properties` 或 `JAVA_HOME`，项目要求 JDK 21）。
  当前环境（macOS arm64，Temurin 21.0.12.1）全新 `--no-daemon` 验证：
  第一轮 `testDebugUnitTest` 45/45；第二轮（世代隔离改造后）54/54 通过、
  `assembleDebug`（83,463,979 B）与 `assembleRelease`（4,676,853 B，versionName=1.0beta/versionCode=10000）成功，
  SHA-256：debug `f81289e9…86aac`、release `05d3e858…451457`；
  `git diff --check` 通过；release dex 32 位十六进制 Key 模式扫描为空。
- **范围**：构建验证

### 当前问题

`gradle.properties` 中存在当前 macOS 不可用的 Windows Java 路径：

```properties
org.gradle.java.home=C:/Users/Souit/scoop/apps/openjdk21/current
```

本轮改用本机 JDK 后，Gradle 又因 Google Maven 依赖下载超时而未完成重新测试。

### 已有证据

工作区中存在历史测试报告，显示当时通过：

- `AmapRouteParserTest`：13 项；
- `RouteProgressEngineTest`：8 项；
- `WalkingAdvanceTest`：4 项；
- `CoordinateTransformTest`：5 项；
- `WifiIdentityHookPolicyTest`：5 项；
- 示例测试：1 项。

这些是历史产物，不等于本轮验证结果。

### 修复/验证要求

- 不要把机器专属的绝对 Java 路径提交到共享配置；
- 使用环境变量、Gradle Toolchain 或本机配置文件解决 JDK 选择；
- 确认项目要求的 JDK 版本；
- 确认 Android Gradle Plugin、Gradle Wrapper 和依赖缓存可用；
- 重新执行：

```bash
./gradlew testDebugUnitTest --no-daemon
./gradlew assembleDebug --no-daemon
./gradlew assembleRelease --no-daemon
```

- 保存新的测试报告和构建产物时间；
- 失败时区分代码错误、JDK错误和网络/依赖下载错误。

### 验收标准

- 单元测试在当前开发环境中重新执行并通过；
- Debug/Release 构建成功；
- 构建不依赖不存在的 Windows 路径；
- `git diff --check` 通过；
- 产物中没有测试 Key。

---

## P1-004：通知体验升级（Live Updates + 小米超级岛）真机验收

> 来源：`MockX完整规划-阶段3-5执行记录.md`（2026-09-19）。代码与静态验证已完成（87/87 单测、双 APK 构建通过、APK 安全检查全绿），以下设备项未验证前本项维持未完成。

### 验收要求（P1-003 的步骤全部仍适用，另加）

**A. Android 16 非小米设备（Live Updates）**

1. 开始步行，确认通知 ongoing、有暂停/停止按钮、进度条与之前一致；
2. 开启系统 Live Updates 开关（设置 → 应用 → MockX → 通知 → Live Updates；或 `adb shell am start -a android.settings.ACTION_APP_NOTIFICATION_PROMOTION_SETTINGS`），确认通知以 Live Update 展示（抽屉顶部/锁屏/状态栏 chip），进度条带 tracker 图标与已走/未走分段着色；
3. 行走中确认状态栏 chip 或通知头部出现 ETA 倒计时；
4. 暂停/继续/停止各一次，确认 chip、按钮、进度即时切换且无旧通知残留；
5. 关闭 Live Updates 开关重启会话，确认退化为普通通知且功能不受影响；
6. 通知更新约 2 秒一次（旧版为每秒），位置推进频率不变。

**B. HyperOS 3 小米设备（超级岛）**

1. 开始步行，确认标准通知正常；
2. 确认超级岛出现（摘要态小岛为应用图标，展开大岛显示状态/距离/进度条）；若未上岛，记录 `adb shell settings get system notification_focus_protocol` 输出与截图（上岛与否均记入证据，属调研预期内未确认项）；
3. 确认约 2 秒一次的进度推进；
4. 暂停/继续/停止各一次，确认岛内容切换、停止后岛消失；
5. 锁屏/AOD 检查内容只含状态与距离，不含坐标、地址、Key；
6. 到达终点后确认"已到达"展示与清理。

**C. 全设备回归**

- UI 页面（地图/设置/目标应用）随 Compose 连带升级（1.6.x→1.11.2、material3 1.4.0）做一轮目视回归；
- 目标 App 仍持续收到动态位置（通知升级不得影响 Hook 链路）。

### 验收标准

- 上述 A/B/C 记录截图或 `dumpsys notification` 输出至本文件；
- 任一退化路径（推广关闭、非小米设备、payload 失败）均不崩溃、不中断步行模拟；
- 完成后勾选下方完成定义中的 P1-004。

---

## 已通过或暂不阻塞的检查

- `git diff --check` 通过。
- 未在源码、资源、规划文档中发现测试 Key 硬编码。
- 已存在高德路线客户端、路线解析器、坐标转换、路线插值和前台 Service 结构。
- 高德官方资料确认步行路线接口为：
  `https://restapi.amap.com/v5/direction/walking`
- 高德文档确认 `origin`/`destination` 为经度在前、纬度在后的坐标参数。
- 高德文档确认 `show_fields` 可请求 polyline，步行 polyline 返回分段坐标点串。

---

## 完成定义

只有以下条件全部满足，才将本追踪文档标记为完成：

- [x] P1-001 时间基准问题已修复并有测试覆盖。（`WalkingStatePolicyTest`）
- [ ] P1-002 停止/失败清理竞态已修复并有时序测试或设备证据。（代码已修复；时序证据依赖 P1-003 设备验收，故暂不勾选）
- [x] P2-001 无 ticker 暂停状态问题已修复并有测试覆盖。（`WalkingCommandPolicyTest`）
- [ ] P1-003 已完成实体设备完整链路验收。
- [ ] P1-004 通知体验升级（Live Updates + 超级岛）真机验收完成。（代码与静态验证已完成，见 `MockX完整规划-阶段3-5执行记录.md`）
- [x] P2-002 当前环境可重复运行测试和构建。（第二轮 54/54 通过；阶段 3-5 轮 87/87 通过，Debug/Release 构建成功）
- [ ] 测试 Key 已从设备设置中清除并在高德控制台重置。
- [ ] 最终 Git diff 不包含完整 Key、私密坐标或设备日志。
