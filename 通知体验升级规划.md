# MockX 步行模拟通知体验升级规划

> 目标项目：`/Volumes/SanDisk/Projects/XposedFakeLocation`
>
> 编写日期：2026-09-19
>
> 文档状态：规划，不在本次任务中实现代码。
>
> 适用功能：Android 官方 Live Updates、Android 低版本前台进度通知、小米 HyperOS 超级岛通知。

---

## 1. 目标与最终结论

### 1.1 目标

为现有高德步行模拟功能增加统一的持续状态展示：

- Android 16 及以上设备使用官方 Live Updates / `Notification.ProgressStyle`；
- Android 9–15 使用现有 `NotificationCompat` 进度通知；
- 支持小米 HyperOS 的设备额外携带官方超级岛扩展参数；
- 所有设备只维护一条通知，避免重复通知和状态不一致；
- 步行 Service、地图 UI、通知、共享 Preference、Xposed Hook 使用同一份会话状态；
- 暂停、继续、停止、到达、失败时，通知与实际状态严格同步。

### 1.2 总体选择

采用“一条通知、三层适配”的方案：

```text
WalkingSimulationService
        ↓
WalkingNotificationState
        ↓
WalkingNotificationBuilder
  ├─ Android 16：Live Update + ProgressStyle
  ├─ 小米 HyperOS：miui.focus.param 超级岛扩展
  └─ 其他设备：标准前台服务进度通知
```

不实现两套独立通知，不创建重复的通知 ID，不让小米专用逻辑替代 Android 通用回退逻辑。

### 1.3 参考规范

实现时重新核对官方文档的当前版本：

- [Android Live Updates](https://developer.android.com/develop/ui/views/notifications/live-update)
- [Android Notification.ProgressStyle](https://developer.android.com/reference/android/app/Notification.ProgressStyle)
- [Android Progress-centric notifications](https://developer.android.com/about/versions/16/features/progress-centric-notifications)
- [小米澎湃 OS 岛通知开发指南](https://dev.mi.com/xiaomihyperos/documentation/detail?pId=2131)

文档中的 API、字段名和系统行为不得仅依据第三方示例推断；如果官方文档发生变化，以当前官方文档和真机行为为准。

---

## 2. 当前项目基线

### 2.1 已有实现

当前项目已经具备：

- `targetSdk = 36`、`compileSdk = 36`、`minSdk = 29`；
- `WalkingSimulationService` 前台服务；
- 位置路线推进、暂停、继续、停止和到达状态；
- 现有通知 ID、通知 Channel、暂停/继续/停止 Action；
- `NotificationCompat.setProgress()` 百分比进度；
- WakeLock、Service 恢复和状态写入策略；
- 多语言资源；
- 当前步行距离、总距离和路线进度；
- 未来扩展多停靠点路线的状态模型基础。

主要文件：

- `app/src/main/java/com/noobexon/xposedfakelocation/manager/walking/WalkingSimulationService.kt`
- `app/src/main/java/com/noobexon/xposedfakelocation/manager/route/WalkingModels.kt`
- `app/src/main/java/com/noobexon/xposedfakelocation/data/repository/PrefrencesRepository.kt`
- `app/src/main/AndroidManifest.xml`
- `app/build.gradle.kts`

### 2.2 当前通知行为

当前 Service 使用：

```kotlin
NotificationCompat.Builder
    .setOngoing(true)
    .setProgress(1000, progress, paused)
    .addAction(pauseOrResume)
    .addAction(stop)
```

这部分必须继续保留作为低版本和非小米设备的可靠回退。

### 2.3 现有验收前提

本功能不能绕过已有步行功能的未完成设备验收：

- `追踪.md` 中的 P1-003 实体设备验收仍需完成；
- 通知层改动必须在真实 LSPosed 设备上验证；
- Android 16 Live Updates 必须在 API 36 设备或模拟器验证；
- 小米超级岛必须在实际支持该功能的 HyperOS 设备验证；
- 通知层不能掩盖位置注入、停止恢复或服务状态问题。

---

## 3. 范围与非目标

### 3.1 本次范围

- 统一通知状态模型；
- Android 16 Live Updates；
- `Notification.ProgressStyle` 路线进度；
- Android 9–15 标准进度通知回退；
- 小米 HyperOS 超级岛通知扩展；
- 通知操作与 Service 命令一致性；
- 多停靠点状态的通知映射接口；
- 通知权限、推广通知设置和前台服务通知兼容；
- 通知构建器单元测试、API 级别测试和真机验收。

### 3.2 不在本次范围

- 不实现新的路线规划算法；
- 不修改高德 Key 管理；
- 不改造 Xposed 定位 Hook 核心逻辑；
- 不新增 MIPush 云推送服务；
- 不使用远程服务器发送超级岛通知；
- 不实现 Live Activity 或 iOS Extension；
- 不实现 Wear OS；
- 不使用 `RemoteViews` 做自定义通知布局；
- 不保证所有 HyperOS 版本和机型都显示超级岛；
- 不把超级岛模板字段做成公开网络配置。

---

## 4. 产品行为定义

### 4.1 通知生命周期

| 会话阶段 | 通知行为 | 可用操作 |
|---|---|---|
| `PLANNING` | 普通短时规划提示，不启动持续步行通知 | 无或取消规划 |
| `READY` | 不启动前台步行通知；地图页面显示准备状态 | 开始 |
| `WALKING` | 持续通知，显示进度、距离、预计剩余时间 | 暂停、停止 |
| `PAUSED` | 持续通知，显示已暂停和当前位置进度 | 继续、停止 |
| `ARRIVED` | 持续通知，显示已到达 | 停止 |
| `STOPPING` | 短暂显示停止中，完成清理后移除 | 无 |
| `FAILED` | 显示失败原因；清理完成后移除或转为普通错误通知 | 返回 App、重新规划 |
| `IDLE` | 不保留步行持续通知 | 无 |

### 4.2 通知内容

最低信息：

- 标题：`模拟步行`；
- 当前阶段：行走中、已暂停、已到达；
- 已走距离；
- 剩余距离；
- 预计剩余时间；
- 当前停靠点名称（未来多停靠点场景）；
- 开始/终点图标或路线进度图标；
- 暂停/继续按钮；
- 停止按钮。

隐私限制：

- 默认不显示精确经纬度；
- 不显示高德 Key；
- 不显示目标 App 包名列表；
- 不显示完整搜索词或私人地点历史；
- 锁屏文案应允许系统隐藏敏感内容；
- 超级岛摘要内容不能包含完整精确地址。

### 4.3 更新频率

- 位置状态仍由 Service 每秒推进；
- 通知不必每秒刷新，默认 1–3 秒更新一次；
- 到达、暂停、继续、停止等状态变化必须立即刷新；
- 不得为了通知刷新频率改变位置推进频率；
- 通知更新失败不能停止位置模拟 Service。

---

## 5. 统一通知状态模型

### 5.1 新增模型

建议新增独立模型，例如：

```kotlin
data class WalkingNotificationState(
    val phase: WalkingPhase,
    val progress: Int,
    val progressMax: Int,
    val travelledMeters: Double,
    val remainingMeters: Double,
    val remainingSeconds: Long?,
    val currentStopIndex: Int?,
    val totalStopCount: Int?,
    val currentStopName: String?,
    val sequence: Long,
    val isPaused: Boolean,
    val isTerminal: Boolean,
)
```

模型要求：

- 只表达通知所需数据；
- 不携带 API Key；
- 不携带完整路线点；
- 不直接持有 Android `Context`；
- 可在 JVM 单元测试中构造；
- `sequence` 单调递增，用于防止通知更新顺序错乱。

### 5.2 进度统一规则

当前单段路线：

```text
progressMax = 1000
progress = travelled / total * 1000
```

未来多停靠点路线：

```text
progressMax = 所有路线段长度之和
progress = 已完成段长度 + 当前段已行走长度
```

不要使用不同通知后端各自计算进度，所有后端必须消费同一个 `WalkingNotificationState`。

### 5.3 阶段映射

通知层不得直接依赖中文显示文本判断状态，必须使用 `WalkingPhase`：

```text
WALKING  -> ongoing + pause action
PAUSED   -> ongoing + resume action
ARRIVED  -> ongoing + no movement action or stop action
STOPPING -> final update then remove
FAILED   -> failure update then cleanup
IDLE     -> notification removed
```

---

## 6. 通知架构

### 6.1 文件组织建议

建议新增：

```text
app/src/main/java/com/noobexon/xposedfakelocation/manager/notification/
├── WalkingNotificationState.kt
├── WalkingNotificationBuilder.kt
├── AndroidLiveUpdateAdapter.kt
├── XiaomiIslandAdapter.kt
├── NotificationCapability.kt
└── NotificationContentFormatter.kt
```

可按项目实际风格合并文件，但职责必须保持分离。

### 6.2 职责划分

#### `WalkingNotificationState`

- 纯 Kotlin 数据模型；
- 负责通知输入，不负责构建 Notification。

#### `NotificationContentFormatter`

- 使用资源字符串生成标题和内容；
- 处理距离、时间、阶段和停靠点文案；
- 不直接访问 Preference；
- 不读取位置原始坐标。

#### `AndroidLiveUpdateAdapter`

- 只处理 API 36+ 官方通知样式；
- 生成 `Notification.ProgressStyle`；
- 生成 progress segments、points 和 tracker icon；
- 不添加小米私有 extras。

#### `XiaomiIslandAdapter`

- 只负责 HyperOS 扩展数据；
- 生成 `miui.focus.param` JSON；
- 不发送网络请求；
- 不依赖 MIPush；
- 在不支持或参数生成失败时静默退化。

#### `WalkingNotificationBuilder`

- 创建通知 Channel；
- 创建统一基础 Builder；
- 注入标准 Action；
- 按 API 版本注入 Android Live Update；
- 按设备能力注入小米扩展；
- 返回最终 `Notification`。

#### `WalkingSimulationService`

- 组装 `WalkingNotificationState`；
- 调用 Builder；
- 调用 `startForeground` 或 `notify`；
- 管理通知更新节流；
- 不直接拼 Xiaomi JSON；
- 不直接计算 Android 16 专属样式细节。

---

## 7. Android 官方 Live Updates 实现

### 7.1 适用条件

只有 API 36+ 才使用 `Notification.ProgressStyle`。官方 Live Update 推广还受系统和用户设置控制，不能假定所有 API 36 设备都会显示为顶部 Live Update。

实现必须保留普通 Notification 作为基础载体。

### 7.2 Manifest 权限

评估并加入：

```xml
<uses-permission
    android:name="android.permission.POST_PROMOTED_NOTIFICATIONS" />
```

同时继续保留：

```xml
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
```

执行时确认 `POST_PROMOTED_NOTIFICATIONS` 在当前 compile/target SDK 中可用。若 AndroidX Core 尚未提供兼容方法，API 36 专属调用必须放在版本分支中，并用平台 API 或兼容扩展实现。

### 7.3 推广通知要求

基础通知需要满足官方条件：

- `ongoing`；
- 设置 `contentTitle`；
- 请求 promoted ongoing；
- Channel 不能是 `IMPORTANCE_MIN`；
- 不使用 `RemoteViews`；
- 不使用 group summary；
- 不设置与推广条件冲突的 colorized/custom 属性；
- 用户关闭推广通知后正常退化为普通通知。

需要在设置页或首次开始步行时提供进入系统通知/推广通知设置的入口，但不能强制用户开启。

### 7.4 `ProgressStyle` 映射

单段路线：

```text
Segment：整段路线
Progress：当前已走距离
Start icon：起点
Tracker icon：行走图标
End icon：目的地
```

多停靠点路线：

```text
Segment 0：起点到停靠点 A
Segment 1：停靠点 A 到停靠点 B
Segment 2：停靠点 B 到终点
Point：每个停靠点
Progress：总累计距离
```

建议：

- `setStyledByProgress(true)`；
- 段颜色使用少量稳定颜色，不根据路线来源动态变化；
- 暂停状态保留当前位置 tracker，但不继续推进 progress；
- 到达状态 progress 固定为总长度；
- 失败和停止不伪造完成进度，应移除或更新为终止状态。

### 7.5 API 36 兼容封装

禁止在低版本类初始化、静态字段或无条件方法体中直接触发 API 36 类，避免验证错误或运行时崩溃。

推荐：

```kotlin
if (Build.VERSION.SDK_INT >= 36) {
    AndroidLiveUpdateAdapter.apply(builder, state)
}
```

如果 Kotlin 编译器对 API 36 平台类产生 lint 或字节码问题：

- 使用 `@RequiresApi(36)`；
- 将实现放到单独对象；
- 通过 `Build.VERSION.SDK_INT` 保护调用；
- 不用反射掩盖真实 API 错误，除非官方兼容库尚未可用且经过测试。

---

## 8. Android 低版本回退

### 8.1 API 29–35

继续使用：

```kotlin
.setProgress(1000, progress, isIndeterminate = false)
```

保留：

- 标准标题和内容；
- ongoing；
- Channel；
- 暂停/继续/停止 Action；
- 点击通知回到 MainActivity；
- `CATEGORY_NAVIGATION`；
- `setOnlyAlertOnce(true)`。

### 8.2 API 33+ 通知权限

- 首次开始步行前检查 `POST_NOTIFICATIONS`；
- 用户拒绝时不阻塞路线规划，但必须阻止无法满足前台服务要求的启动路径或显示明确提示；
- 已有项目的权限请求逻辑继续复用；
- 权限拒绝后的 UI 不得显示“后台通知已启用”；
- 通知被用户关闭时，Service 仍需按系统前台服务规则处理，不能默默继续未知状态。

### 8.3 Channel

- 不使用 `IMPORTANCE_MIN`；
- 现有 Channel ID 保持兼容，避免升级后出现重复通知渠道；
- 如果需要调整 importance，必须考虑用户已经创建的 Channel 不能由 App 静默覆盖；
- 在设置页显示通知渠道状态和跳转系统设置入口。

---

## 9. 小米 HyperOS 超级岛实现

### 9.1 接入原则

小米官方提供的客户端方案是：

1. 先创建普通 Notification；
2. 将岛通知 JSON 放入 `notification.extras`；
3. 使用 `miui.focus.param` 作为扩展 Key；
4. 支持岛通知的设备由系统呈现岛样式；
5. 不支持的设备继续显示普通通知。

因此 Xiaomi adapter 只能是增强层，不能决定 Service 是否运行，也不能成为唯一通知通道。

### 9.2 设备检测

第一版不建议只通过厂商字符串决定是否使用扩展。建议：

- 通过 `Build.MANUFACTURER`、`Build.BRAND`、系统属性做低成本判断；
- 即使检测不是小米，也可以配置为尝试添加兼容 extras，但默认优先在小米设备添加；
- 任何未知机型不得因为 Xiaomi adapter 失败而影响基础通知；
- 不使用隐藏 API；
- 不要求用户安装小米 SDK；
- 不引入 MIPush。

最终是否显示超级岛只能通过设备实际展示验证，不能以 `extras` 写入成功作为功能验收。

### 9.3 `miui.focus.param`

新增独立 JSON 构建器，避免散落字符串拼接：

```kotlin
interface XiaomiIslandAdapter {
    fun apply(notification: Notification, state: WalkingNotificationState): Notification
}
```

建议第一版只支持最小字段：

- 协议版本；
- `business` 场景标识；
- 是否可更新；
- 摘要态标题和内容；
- 展开态标题和内容；
- 当前状态；
- 已走距离；
- 剩余距离；
- `sequence` 更新序号；
- 停止/暂停/继续对应的交互能力（若官方模板允许）。

不要第一版加入：

- 远程图片；
- AOD 图片；
- 未确认的模板字段；
- 任意自定义 URL；
- 复杂分享数据；
- 依赖 MIPush 的字段。

### 9.4 状态和更新顺序

超级岛更新必须使用单调递增 `sequence`：

```text
每次状态或进度更新：sequence += 1
```

停止时：

- 先发送终止/取消语义（如果官方模板支持）；
- 再移除本地通知；
- 不再发送旧序号更新。

如果 Xiaomi adapter 生成 JSON 失败：

- 记录脱敏错误；
- 继续发送标准通知；
- 不改变步行状态；
- 不重启 Service。

### 9.5 隐私

超级岛常驻在状态栏、锁屏或 AOD 附近，内容必须比 App 内页面更保守：

- 显示“模拟步行”或模糊地点名；
- 默认不显示精确地址和坐标；
- 不显示目标包名；
- 不显示 Key、路线原始 JSON 或内部 ID。

---

## 10. 同一通知同时支持两套能力

### 10.1 统一构建流程

```text
1. 读取当前 WalkingSession 状态
2. 生成 WalkingNotificationState
3. 创建 NotificationChannel
4. 创建标准 NotificationCompat.Builder
5. 注入标题、内容、ongoing 和 Action
6. API 36+ 注入 ProgressStyle
7. 小米设备注入 miui.focus.param
8. 构建 Notification
9. startForeground/notify
```

### 10.2 兼容规则

| 环境 | 标准通知 | Android Live Update | 小米超级岛 |
|---|---:|---:|---:|
| Android 9–15 非小米 | 是 | 否 | 否 |
| Android 9–15 小米 | 是 | 否 | 尝试 |
| Android 16 非小米 | 是 | 尝试 | 否 |
| Android 16 小米 | 是 | 尝试 | 尝试 |
| 权限被拒绝 | 按系统规则处理 | 不保证推广 | 不保证展示 |
| Adapter 失败 | 继续 | 退化 | 退化 |

### 10.3 不允许的实现

- 为 Live Update 和超级岛创建两个不同通知 ID；
- 让 Xiaomi adapter 自行调用 `notify()` 发送第二条通知；
- 使用 `RemoteViews` 破坏 Live Update 资格；
- 使用未经验证的 MIUI 隐藏 API；
- 让厂商判断影响路线推进；
- 将通知展示成功当作位置模拟成功；
- 在通知 JSON 中写入精确位置或敏感 Key。

---

## 11. Service 改造

### 11.1 `WalkingSimulationService` 改动

保留 Service 的职责：

- 路线推进；
- 状态写入；
- 前台服务生命周期；
- WakeLock；
- 终态清理。

新增/调整：

- 组装 `WalkingNotificationState`；
- 通过统一 Builder 创建通知；
- 增加通知更新节流；
- 用 `sequence` 防止异步通知更新倒序；
- 状态写入完成后再更新对应终态通知；
- Stop/fail 清理顺序不能被通知适配器破坏。

### 11.2 通知更新节流

建议：

```text
位置 tick：每秒
通知更新：最多每 2 秒一次
状态变化：立即更新
到达/停止/失败：立即更新并执行终态流程
```

如果当前距离/剩余距离没有可见变化，可以跳过通知刷新，但不能跳过共享位置状态更新。

### 11.3 通知异常

通知构建失败不应直接让 Service 崩溃：

```text
Live Update 构建失败
    → 标准 NotificationCompat
超级岛 JSON 失败
    → 不加 Xiaomi extras
标准通知失败
    → 记录错误并进入已有前台服务失败处理
```

不得捕获所有异常后假装通知已发送。

---

## 12. 权限与设置 UI

### 12.1 必要权限

核对并维护：

```xml
POST_NOTIFICATIONS
POST_PROMOTED_NOTIFICATIONS
FOREGROUND_SERVICE
FOREGROUND_SERVICE_LOCATION
WAKE_LOCK
```

其中：

- `POST_NOTIFICATIONS` 是运行时权限；
- `POST_PROMOTED_NOTIFICATIONS` 按 Android 官方要求作为推广通知能力声明并按当前系统行为处理；
- 前台服务位置权限仍按现有启动流程申请和验证。

### 12.2 设置页新增内容

可新增“通知体验”设置组：

- 步行通知开关；
- 请求 Android Live Update 推广入口；
- 小米超级岛增强开关；
- 锁屏是否显示路线摘要；
- 通知更新频率（默认/省电）；
- 恢复默认通知设置。

默认值建议：

- 标准步行通知：开启；
- Android Live Update：自动尝试；
- 小米超级岛：自动尝试；
- 精确地点展示：关闭；
- 通知频率：标准 2 秒。

### 12.3 不应暴露的设置

不要让普通设置页直接编辑：

- `miui.focus.param` 原始 JSON；
- Android 通知模板版本；
- 内部通知 ID；
- sequence；
- route JSON；
- API Key。

---

## 13. 子代理任务划分

以下任务可以使用子代理，但必须使用独立 worktree 或只读审计范围，主代理最终整合和验收。

### 13.1 可并行只读任务

#### [子代理-只读 A] Android Live Updates API 审计

范围：

- 核对 Android 官方 Live Updates 当前要求；
- 核对 API 36 `Notification.ProgressStyle` 方法和兼容写法；
- 核对推广通知权限和设置跳转；
- 核对 `NotificationCompat` 当前版本是否提供 `setRequestPromotedOngoing`；
- 输出 API 级别矩阵和风险，不修改生产文件。

交付：

- 官方文档链接；
- 可用 API 列表；
- 低版本保护建议；
- 需要主代理决定的兼容点。

#### [子代理-只读 B] 小米超级岛官方契约审计

范围：

- 核对小米官方岛通知文档；
- 识别 `miui.focus.param`、模板、业务字段和 sequence 约束；
- 区分客户端本地通知和 MIPush 方案；
- 判断当前步行场景可用的最小模板；
- 不修改生产文件。

交付：

- 官方字段说明；
- 当前已知设备/系统限制；
- 最小可用 JSON 草案；
- 未确认字段清单。

#### [子代理-只读 C] 当前通知/Service 依赖审计

范围：

- 审计 `WalkingSimulationService` 的生命周期、通知刷新和清理；
- 检查现有线程/协程与通知更新的竞态；
- 检查现有权限和 Channel 行为；
- 输出最小改动点，不修改生产代码。

交付：

- 调用链；
- 风险清单；
- 建议测试点；
- 不能并行修改的共享文件列表。

#### [子代理-只读 D] HyperOS 设备测试矩阵设计

范围：

- 设计 Android 16 通知测试矩阵；
- 设计小米 HyperOS 不同状态和锁屏/息屏场景矩阵；
- 设计标准通知回退检查；
- 不需要真实设备时只输出测试方案；有设备时可执行只读检查。

### 13.2 可并行可写任务

必须使用独立 worktree，且写入范围不能重叠。

#### [子代理-可写 A] 纯 Kotlin 通知状态与格式化

写入范围：

```text
app/src/main/java/.../manager/notification/WalkingNotificationState.kt
app/src/main/java/.../manager/notification/NotificationContentFormatter.kt
app/src/test/java/.../manager/notification/NotificationContentFormatterTest.kt
```

不得修改：

- `WalkingSimulationService.kt`；
- Manifest；
- Settings；
- `project` 之外的共享模型。

#### [子代理-可写 B] Android 16 ProgressStyle 适配器

写入范围：

```text
app/src/main/java/.../manager/notification/AndroidLiveUpdateAdapter.kt
app/src/test/java/.../manager/notification/AndroidLiveUpdateAdapterTest.kt
```

要求：

- API 36 安全保护；
- 不能直接启动通知；
- 不修改 Service；
- 明确平台 API 与测试边界。

#### [子代理-可写 C] XiaomiIsland JSON 适配器

写入范围：

```text
app/src/main/java/.../manager/notification/XiaomiIslandAdapter.kt
app/src/main/java/.../manager/notification/XiaomiIslandPayload.kt
app/src/test/java/.../manager/notification/XiaomiIslandAdapterTest.kt
```

要求：

- 只生成 extras/payload；
- 不调用 `NotificationManager.notify()`；
- 不放入真实 Key、坐标和隐私数据；
- 未确认字段通过最小模板隔离。

#### [子代理-可写 D] 测试桩和能力矩阵

写入范围：

```text
app/src/test/java/.../manager/notification/
```

内容：

- API 级别能力判断测试；
- 标准回退测试；
- phase 到 action 的映射测试；
- sequence 单调性测试；
- 失败退化测试。

不得修改生产代码。

### 13.3 必须由主代理完成的任务

以下任务不要交给多个子代理同时修改：

- `WalkingSimulationService.kt` 最终接入；
- `AndroidManifest.xml` 权限和 Service 配置；
- `app/build.gradle.kts`、Version Catalog 和依赖升级；
- `gradle.properties`；
- Settings 页面和设置持久化；
- 共享 Preference schema；
- 通知 ID、Channel ID 和最终生命周期；
- 多停靠点路线与通知模型整合；
- 最终 APK 构建；
- 真机验收；
- `追踪.md` 状态更新；
- Key 清理和安全复核。

### 13.4 子代理整合规则

主代理接收子代理成果后必须：

1. 查看实际 diff；
2. 检查是否越过写入范围；
3. 检查是否引入新依赖；
4. 检查是否使用未经证实的小米字段；
5. 运行单元测试；
6. 统一资源命名和多语言；
7. 决定是否 cherry-pick 或手工移植；
8. 不直接接受“已完成”描述作为验收证据。

---

## 14. 实施阶段与依赖关系

```text
阶段 0：官方契约和当前基线审计
  ├─ Android API 审计 [子代理-只读]
  ├─ 小米契约审计 [子代理-只读]
  └─ 当前 Service 审计 [子代理-只读]
        ↓
阶段 1：纯 Kotlin 通知状态/格式化/测试
  ├─ 状态模型 [子代理-可写]
  ├─ Android 适配器 [子代理-可写]
  └─ 小米适配器 [子代理-可写]
        ↓
阶段 2：主代理整合通知 Builder 和 Service
        ↓
阶段 3：权限、设置、国际化和回退
        ↓
阶段 4：自动测试、构建和 APK 安全检查
        ↓
阶段 5：Android 16 + 小米 HyperOS + 普通 Android 真机验收
        ↓
阶段 6：更新追踪文档和决定是否启用默认增强
```

阶段 1 的三个可写任务可以并行，但必须避免共享文件冲突。阶段 2 开始后暂停对通知包的并行写入，统一由主代理整合。

---

## 15. 测试计划

### 15.1 单元测试

至少覆盖：

- `WalkingPhase` 到通知文案映射；
- `WalkingPhase` 到 Action 映射；
- 进度 0、半程、终点；
- 总距离为 0 的保护；
- 剩余时间格式化；
- 暂停不推进进度；
- 到达固定 100%；
- sequence 单调递增；
- sequence 倒序更新被拒绝；
- 多停靠点 segments/points 映射；
- 非小米不注入或安全忽略 Xiaomi extras；
- Xiaomi payload 不包含坐标/Key；
- JSON 转义和字段缺失；
- Xiaomi payload 构建失败时标准通知仍可构建；
- API 35 不触发 API 36 类路径；
- API 36 能构建 ProgressStyle 输入数据；
- Live Update 与标准进度输入保持同一 progress。

### 15.2 Android 仪器测试

如果环境允许，增加：

- Notification Channel 创建；
- `POST_NOTIFICATIONS` 拒绝/允许；
- API 36 Notification 构建；
- 标准通知 Action PendingIntent；
- 通知更新 ID 一致；
- 停止后通知移除；
- Service 被重建后的通知恢复；
- Xiaomi extras 注入不会破坏普通 Notification。

### 15.3 Android 16 测试

在 API 36 设备或模拟器：

1. 开始步行；
2. 检查通知是否为 ongoing；
3. 检查是否出现 Live Update/推广展示；
4. 关闭推广通知权限，确认退化为普通通知；
5. 展开通知，确认进度、起点、终点和 tracker；
6. 暂停、继续、停止；
7. 锁屏和返回桌面；
8. 确认通知更新不重复、不乱序；
9. 到达终点后确认终态清理。

### 15.4 小米 HyperOS 测试

在真实支持超级岛的 Xiaomi/HyperOS 设备：

1. 开始模拟步行；
2. 检查普通通知是否正常；
3. 检查是否显示超级岛；
4. 检查摘要态和展开态；
5. 检查 2 秒左右进度更新；
6. 检查暂停/继续；
7. 检查停止后超级岛是否消失；
8. 检查到达后是否显示已到达；
9. 检查锁屏/AOD 隐私内容；
10. 关闭通知权限后确认 App 不崩溃；
11. 检查 Xiaomi extras 不影响其他厂商设备。

### 15.5 普通 Android 回归

至少在一个非小米 Android 设备验证：

- Android 9–15 标准通知；
- Android 16 非小米 Live Update；
- 暂停/继续/停止；
- Service 重建；
- 通知权限拒绝；
- 电池优化或后台限制；
- 目标 App 仍收到动态位置；
- 通知失败不影响位置模拟；
- 停止后旧通知和旧动态位置均清理。

### 15.6 与原功能联合验收

必须联合验证：

- 高德路线规划；
- GCJ-02/WGS-84 转换；
- Xposed 动态位置；
- Wi-Fi/基站防泄漏；
- 前台 Service；
- 通知；
- 暂停/继续/停止；
- 到达；
- 锁屏；
- 目标 App。

通知显示正确不等于位置模拟正确，位置模拟正确也不等于终态清理正确。

---

## 16. 性能与可靠性要求

- 通知更新不能阻塞路线推进；
- JSON 构建不能放在主线程的大循环中执行；
- Xiaomi JSON 只在通知刷新时生成；
- 不每秒序列化完整路线；
- Notification Builder 失败要可回退；
- 不创建额外长期线程；
- 不改变现有状态写入串行策略；
- 进度更新顺序必须通过 sequence 或统一串行队列保证；
- Service 停止前必须完成共享状态清理；
- 通知清理失败不能导致旧动态定位状态遗留；
- 避免每秒唤醒多个通知或厂商适配任务。

建议记录非敏感诊断指标：

- 当前通知后端：standard/live_update/xiaomi/both；
- 最后通知序号；
- 最后通知更新时间；
- 适配器失败次数；
- 当前 API level 和厂商类别。

不得记录：

- API Key；
- 精确坐标；
- 完整路线；
- 目标 App 详细列表；
- 个人地点名。

---

## 17. 安全与隐私

1. 不在通知、超级岛 JSON、Logcat 或异常中输出高德 Key。
2. 不在通知显示精确经纬度。
3. 不将 Xiaomi payload 上传到远端。
4. 不使用 MIPush 发送本地步行状态。
5. 不把 `miui.focus.param` 作为外部可编辑配置。
6. 不为通知功能增加远程控制入口。
7. 不使用隐藏 MIUI API 或反射访问系统内部服务。
8. 通知 Action 必须是显式 PendingIntent，避免第三方应用伪造控制。
9. 外部广播控制仍受现有开关限制。
10. 通知停止必须和 `walking_enabled=false`、`is_playing=false` 的清理顺序一致。
11. Key 测试完成后清除设备存储并在高德平台重置。
12. Release APK 和 Git diff 扫描不得发现 Key、私密坐标或测试日志。

---

## 18. 构建与依赖策略

### 18.1 依赖原则

- 优先使用 Android 平台 API 和现有 AndroidX Core；
- 不为 Xiaomi 超级岛引入小米 SDK；
- 不为 Live Updates 引入第三方通知库；
- 只有当前 AndroidX Core 版本确实缺少必要兼容方法时才评估升级；
- 依赖升级必须由主代理完成并记录变更原因；
- 不把厂商专属依赖放到 Xposed Hook 进程。

### 18.2 构建检查

执行：

```bash
./gradlew testDebugUnitTest --no-daemon
./gradlew assembleDebug --no-daemon
./gradlew assembleRelease --no-daemon
git diff --check
```

使用项目要求的 JDK 21，不恢复机器专属 Windows Java 路径。

### 18.3 APK 检查

- Debug/Release 均成功；
- Manifest 含必要权限和 Service；
- API 36 类只在安全版本分支使用；
- APK 不包含完整高德 Key；
- APK 不包含测试日志、路线 JSON 或私密坐标；
- 不存在重复通知 ID；
- 不引入未使用的厂商 SDK。

---

## 19. 分阶段交付

### 阶段 0：官方契约与当前基线

交付：

- Android 官方 API 审计；
- 小米官方参数审计；
- 当前通知/Service 调用链；
- API/厂商兼容矩阵；
- 未确认字段列表。

可使用：`[子代理-只读 A/B/C/D]`。

### 阶段 1：通知纯模型

交付：

- `WalkingNotificationState`；
- 文案格式化；
- phase/action/progress 测试；
- 多停靠点映射接口。

可使用：`[子代理-可写 A/D]`。

### 阶段 2：Android Live Update

交付：

- API 36 `ProgressStyle` 适配；
- 权限和能力判断；
- 低版本安全回退；
- API 36 单元/仪器测试。

可使用：`[子代理-可写 B]`，主代理负责最终编译和接入。

### 阶段 3：小米超级岛

交付：

- Xiaomi payload 模型；
- extras 注入适配器；
- 不支持设备安全回退；
- Xiaomi 真机测试记录。

可使用：`[子代理-可写 C]`，主代理负责官方字段复核和最终接入。

### 阶段 4：统一 Builder 和 Service 整合

交付：

- 一条通知；
- 同时支持官方 Live Update 和 Xiaomi extras；
- 统一 Action、进度、sequence；
- 停止/失败/到达的可靠清理。

必须由：`[主代理]` 完成。

### 阶段 5：设置与本地化

交付：

- 通知体验设置；
- 权限引导；
- 中英文资源；
- 锁屏隐私选项。

必须由主代理整合，UI 文案扫描可由 `[子代理-只读]` 协助。

### 阶段 6：构建和实体设备验收

交付：

- 45 项原有测试无回归；
- 新增通知测试全部通过；
- Debug/Release 构建成功；
- Android 16 设备证据；
- HyperOS 设备证据；
- 普通 Android 回退证据；
- 追踪文档更新。

必须由：`[主代理]` 完成最终验收。

---

## 20. 回滚策略

### Live Updates 回滚

- 保留标准 `NotificationCompat`；
- 关闭 `ProgressStyle` 注入；
- 不删除原通知 Action；
- 不改变路线 Service。

### 小米超级岛回滚

- 删除或停用 `miui.focus.param` extras；
- 保留同一通知 ID 和标准内容；
- 不卸载、不引入小米 SDK；
- 不改变步行状态。

### 整体回滚

- 通过独立提交或 revert 回滚；
- 不使用 `git reset --hard` 覆盖用户现有改动；
- 不删除路线、偏好或用户 Key 设置；
- 回滚后仍可使用普通前台通知和位置模拟。

---

## 21. 完成定义

以下条件全部满足后才可标记完成：

- [ ] Android 官方 Live Updates 适配完成。
- [ ] API 36 以下标准通知回退通过。
- [ ] 小米超级岛 payload 只使用已核实的官方字段。
- [ ] 小米设备实际显示或明确记录系统不支持原因。
- [ ] 同一通知 ID 同时支持标准通知、Live Update 和超级岛增强。
- [ ] 不创建重复通知或重复 Service。
- [ ] 暂停、继续、停止、到达、失败状态一致。
- [ ] Service 重建后通知与共享状态一致。
- [ ] sequence 防止通知倒序更新。
- [ ] 通知适配失败不影响位置推进或终态清理。
- [ ] API 36、Android 9–15、HyperOS 至少各有一组运行证据。
- [ ] 单元测试和构建全部通过。
- [ ] `git diff --check` 通过。
- [ ] APK 和 Git diff 不包含 Key、精确坐标和调试日志。
- [ ] 测试 Key 已清除并在高德控制台重置。
- [ ] `追踪.md` 已补充通知功能验收结果。

---

## 22. 推荐实施顺序

1. 先完成官方 Android API 和 Xiaomi 官方字段审计，不直接复制第三方示例。
2. 建立纯 Kotlin 通知状态模型和测试。
3. 先实现标准通知抽象，保证 Android 9–15 不回归。
4. 加入 API 36 `ProgressStyle`，在 Android 16 验证。
5. 加入 Xiaomi extras 适配器，在 HyperOS 真机验证。
6. 最后由主代理整合到 `WalkingSimulationService`。
7. 进行权限、锁屏、Service 重建、停止清理和隐私验收。
8. 更新 `追踪.md`，只有实体设备证据充分后才标记完成。

最重要的决策：**通知增强是展示层，不得成为定位状态机的第二数据源。** 路线推进和 Xposed 注入状态仍以现有 Service/共享 Preference 状态为唯一事实来源。
