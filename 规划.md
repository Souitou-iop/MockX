# 高德步行路线模拟功能实施规划

> 文档用途：提供给其他模型/开发者执行实现。
> 
> 目标项目：`/Volumes/SanDisk/Projects/XposedFakeLocation`
> 
> 编写日期：2026-09-18
> 
> 当前状态：仅完成规划，不在本次文档编写中实现功能。

---

## 1. 目标与边界

### 1.1 目标

在现有固定点虚拟定位基础上，新增“模拟步行”能力：

1. 用户在地图上选择目的地。
2. 使用设备当前真实位置作为起点。
3. 调用高德 Web 服务路径规划 2.0 的步行路线接口。
4. 在地图上展示规划出的步行路线。
5. 用户选择步行速度后开始模拟。
6. 模拟位置沿路线移动，而不是瞬移到固定坐标。
7. 目标应用通过现有 Xposed Hook 获得不断变化的 `Location`。
8. 切换到其他 App 或锁屏后，模拟仍尽可能持续运行。
9. 支持暂停、继续、停止和到达终点。
10. 不破坏现有固定位置、目标 App 过滤、系统服务 Hook、Wi-Fi/基站防泄漏功能。

### 1.2 明确不做

第一版不包含以下内容：

- 不接入百度路线 API。
- 不实现完整导航语音、转向提示或导航 UI。
- 不实现用户自定义多段路线编辑器。
- 不实现路线历史管理。
- 不实现进程被系统彻底杀死后的断点续走。
- 不把高德 Key 写入源码、资源文件、Git、默认配置或发布 APK。
- 不让 Xposed Hook 直接发网络请求或计算路线。
- 不用高德地图瓦片接口代替路线接口。

---

## 2. 当前项目事实

### 2.1 已有能力

当前项目已经具备：

- Android Manager App 与 Xposed Hook 两部分代码。
- osmdroid 地图显示。
- 高德矢量和卫星瓦片图源：
  - `app/src/main/java/com/noobexon/xposedfakelocation/manager/ui/map/CustomTileSources.kt`
- GCJ-02 与 WGS-84 相互转换：
  - `app/src/main/java/com/noobexon/xposedfakelocation/manager/ui/map/CoordinateTransform.kt`
- 地图点选、目标坐标保存和固定位置启动流程。
- `PreferencesRepository` 的远程共享偏好读写机制。
- Xposed 侧统一构造虚拟 `Location` 的逻辑：
  - `app/src/main/java/com/noobexon/xposedfakelocation/xposed/utils/LocationUtil.kt`
- 系统服务、应用定位 API 和位置分发 Hook。
- 现有 `ControlReceiver` 外部控制广播机制。
- `INTERNET` 与网络状态权限。
- Gson 依赖，可用于路线响应和状态 JSON 序列化。

### 2.2 当前限制

目前定位状态以一个固定点为主：

```text
PreferencesUtil.getLastClickedLocation()
    -> LocationUtil.updateLocation()
    -> LocationUtil.createFakeLocation()
    -> 各类 Location Hook 返回虚拟位置
```

现有速度字段只用于填充 `Location.speed`，不会使坐标随时间变化。

当前 Manifest 没有前台服务声明和通知相关权限，因此后台持续模拟需要补充 Android Service 方案。

### 2.3 坐标系约束

- 高德地图瓦片和高德路线数据通常使用 GCJ-02。
- Android GPS `Location` 应按 WGS-84 处理。
- 高德路线点进入定位注入链路前必须转换为 WGS-84。
- 地图绘制路线时，如果地图图源是高德 GCJ-02，应把 WGS-84 路线点转换回 GCJ-02 后绘制。
- 不能把高德返回的 GCJ-02 坐标直接写入虚拟 GPS `Location`。
- 不能把已转换为 WGS-84 的点再次重复转换。

---

## 3. 总体架构

```text
┌──────────────────────────────┐
│ Manager App                   │
│                              │
│ 地图选点                     │
│     ↓                        │
│ 获取真实起点                 │
│     ↓                        │
│ 高德 Web API                 │
│     ↓                        │
│ 解析 GCJ-02 路线             │
│     ↓                        │
│ 转换为 WGS-84                │
│     ↓                        │
│ 路线预览                     │
│     ↓                        │
│ 前台 Walking Service         │
│     ↓                        │
│ 每秒推进当前位置             │
│     ↓                        │
│ SharedPreferences 状态       │
└──────────────┬───────────────┘
               │
               ▼
┌──────────────────────────────┐
│ Xposed Hook / system_server  │
│                              │
│ 读取动态 WGS-84 坐标         │
│ 构造 Location                │
│ 注入目标 App                 │
└──────────────────────────────┘
```

核心原则：

- 路线规划、计时、插值和状态控制全部由 Manager App 完成。
- Xposed 侧只负责读取共享状态并返回位置。
- 动态位置必须与固定位置模式有清晰的优先级和回退规则。
- 不能依赖目标 App 的定位请求频率来推进路线；路线推进必须由前台服务独立负责。

---

## 4. 高德 Key 管理

### 4.1 选择的方式

采用“设置页输入 Key”的方案：

- 用户在设置页输入自己的高德 Web 服务 Key。
- Key 保存到现有 Manager App 的设置存储中。
- 路线请求时读取 Key。
- 不把 Key 作为源码常量。
- 不把 Key 写入 `BuildConfig`、`strings.xml`、Manifest 或 CI 配置。
- 不在日志中输出完整 Key。

本次测试使用的 Key 只用于本地手工验证，开发完成后应在高德控制台重置；规划文档和代码仓库不得保存该 Key。

### 4.2 新增设置项

建议新增常量：

```kotlin
const val KEY_AMAP_WEB_SERVICE_KEY = "amap_web_service_key"
const val DEFAULT_AMAP_WEB_SERVICE_KEY = ""
```

建议在 `PreferencesRepository` 增加：

```kotlin
fun getAmapWebServiceKeyFlow(): Flow<String>
suspend fun saveAmapWebServiceKey(value: String)
fun getAmapWebServiceKey(): String
```

保存时：

- `trim()`；
- 空字符串表示未配置；
- 不在保存日志中打印值；
- 不通过外部广播接收 Key。

### 4.3 设置页交互

在现有地图设置或 API 设置区域增加：

- 高德 Web 服务 Key 输入框；
- 密码/隐藏输入模式；
- 保存后显示“已配置”而不是回显完整 Key；
- 清除按钮；
- 说明该 Key 仅用于步行路径规划；
- 错误信息不得包含完整 URL 查询串或 Key。

如果设置页当前只支持 `SettingEntry` 类型，应复用已有设置组件，不新增第三方 UI 依赖。

---

## 5. 高德路径规划客户端

### 5.1 接口选择

使用高德 Web 服务路径规划 2.0 的步行接口：

```text
GET https://restapi.amap.com/v5/direction/walking
```

实际参数名称和响应结构必须以高德当前官方文档为准，执行时不要根据本文件臆造字段。至少确认：

- Key 参数名称；
- 起点和终点参数格式；
- 坐标类型/坐标系参数；
- 步行路径数组字段；
- 路段 polyline 字段；
- 错误码和错误信息字段；
- 配额或签名要求。

### 5.2 客户端实现约束

建议新增独立的 `AmapWalkingRouteClient`，放在 Manager App 侧，例如：

```text
app/src/main/java/com/noobexon/xposedfakelocation/manager/route/
```

实现要求：

- 使用现有 Gson。
- 优先使用 JDK/Android 原生 `HttpURLConnection`；除非项目已有可复用网络库，否则不要新增 Retrofit、OkHttp 等依赖。
- 所有网络操作运行在 `Dispatchers.IO`。
- 设置连接超时和读取超时。
- 正确关闭输入流和连接。
- 对 HTTP 非 2xx、API 业务错误、JSON 解析错误分别处理。
- 不把完整请求 URL 写入日志。
- 错误类型使用可供 UI 显示的安全枚举或脱敏消息。

建议接口：

```kotlin
interface WalkingRouteClient {
    suspend fun planWalkingRoute(
        apiKey: String,
        origin: GeoPoint,
        destination: GeoPoint,
    ): Result<WalkingRoute>
}
```

### 5.3 响应解析

路线解析流程：

1. 校验高德返回状态。
2. 读取推荐路径。
3. 读取所有需要的步行路段。
4. 读取每个路段的 polyline。
5. 按 `;` 分割经纬度点。
6. 将字符串安全转换为 `Double`。
7. 丢弃非法、非有限、越界坐标。
8. 合并相邻路段。
9. 去除相邻重复点。
10. 确认至少存在两个有效点。
11. 计算路线总距离和累计距离。
12. 将 GCJ-02 点转换为 WGS-84。
13. 保存原始 API 距离作为校验数据，但移动计算以解析后的轨迹点距离为准。

注意：

- 不要只取路线起点和终点，否则会变成直线移动。
- 不要把路线 polyline 的经纬度顺序写反。
- 不要把分段之间的重复点造成的零长度段当作错误。
- 路线点数量过大时应限制内存和持久化大小，但不能未经验证粗暴抽稀导致路线明显偏离道路。

### 5.4 数据模型建议

```kotlin
data class WalkingRoute(
    val origin: Coordinate,
    val destination: Coordinate,
    val points: List<Coordinate>, // 统一保存为 WGS-84
    val totalDistanceMeters: Double,
    val expectedDurationSeconds: Int?,
    val provider: String = "amap",
)

data class Coordinate(
    val latitude: Double,
    val longitude: Double,
)
```

如果项目已有 `GeoPoint` 作为 UI 地图坐标，可在 API/定位模型中使用项目内部 `Coordinate`，避免混淆 GCJ-02 与 WGS-84。类型或字段命名必须明确坐标系。

---

## 6. 真实起点获取

### 6.1 起点规则

第一版固定采用：

```text
设备当前真实位置 -> 地图选中的目标点
```

不能使用当前虚拟位置作为起点，否则重复规划时会从伪造位置继续产生偏移。

### 6.2 起点来源

优先使用 Manager App 已获得的真实位置：

- 复用现有 `MapViewEffects` / `LocationManager` 获取逻辑；
- 如果真实位置不存在，提示用户先开启定位权限并等待定位；
- 不从目标 App 的虚拟定位回调中读取起点；
- 不用 `LocationUtil` 的当前虚拟坐标作为真实起点。

### 6.3 起点坐标转换

如果地图当前使用高德图源：

- 地图点击点通常需要从 GCJ-02 反算为 WGS-84 后存储；
- 路线 API 的起终点参数按官方接口要求提供；
- 调用前确认 API 所需坐标系，必要时把 WGS-84 转回 GCJ-02；
- 返回 polyline 后再次统一转换为 WGS-84 保存。

应在接口适配层集中处理转换，不要在 UI、Service 和 Hook 多处重复转换。

---

## 7. 路线与步行状态模型

### 7.1 状态枚举

建议：

```kotlin
enum class WalkingPhase {
    IDLE,
    PLANNING,
    READY,
    WALKING,
    PAUSED,
    ARRIVED,
    STOPPING,
    FAILED,
}
```

### 7.2 运行状态

建议新增 `WalkingSessionState`：

```kotlin
data class WalkingSessionState(
    val phase: WalkingPhase,
    val route: WalkingRoute?,
    val distanceTravelledMeters: Double,
    val currentLatitude: Double?,
    val currentLongitude: Double?,
    val speedMetersPerSecond: Float,
    val bearingDegrees: Float?,
    val startedAtEpochMillis: Long?,
    val updatedAtEpochMillis: Long?,
    val errorCode: String?,
)
```

### 7.3 状态持久化

需要区分：

- 高频运行状态：服务内存 + 共享 Preference 中的当前坐标；
- 低频路线状态：路线和会话元数据可以 JSON 保存；
- 不要每个 tick 都写完整路线 JSON。

建议新增共享字段：

```text
walking_enabled
walking_phase
walking_route_json
walking_current_latitude
walking_current_longitude
walking_distance_travelled
walking_total_distance
walking_speed
walking_bearing
walking_started_at
walking_updated_at
walking_error_code
```

写入原则：

- `walking_current_*`、`walking_speed`、`walking_bearing`、`walking_updated_at` 每次推进更新；
- `walking_route_json` 只在规划成功、停止或路线变更时更新；
- 使用 `commit` 或现有远程 Preference 写入方式，确保 Xposed 进程能及时读到；
- 如果现有 Preference 服务连接不可用，必须显示明确错误，不要静默假装路线已启动。

### 7.4 与固定位置模式的优先级

明确采用：

```text
有效 WALKING/PAUSED/ARRIVED 动态状态
    > 固定位置 last_clicked_location
    > 原始真实 Location
```

但 `PAUSED` 必须返回暂停时的动态坐标，不能回退到固定点。

`ARRIVED` 可以继续返回终点，直到用户停止。

`STOPPING` 和 `IDLE` 时，按现有停止逻辑恢复真实位置或关闭伪装，具体行为必须与当前固定位置流程保持一致。

---

## 8. 路线推进算法

### 8.1 速度

第一版提供三个预设：

```text
轻松：1.0 m/s
正常：1.4 m/s
快速：1.8 m/s
```

同时允许后续扩展为自定义速度，但第一版不需要自由输入复杂速度范围。

### 8.2 推进方式

路线点转换为 WGS-84 后：

1. 计算每两个相邻点之间的距离。
2. 生成累计距离数组。
3. 每次 tick 使用真实时间差：
   
   ```text
   distance += speed * elapsedSeconds
   ```

4. 在累计距离数组中查找当前所在路段。
5. 对该路段按比例线性插值。
6. 计算当前位置和下一位置之间的 bearing。
7. 写入共享动态位置。

### 8.3 Tick 周期

- 默认每 1000 ms 推进一次。
- 时间差使用单调时钟或 `SystemClock.elapsedRealtime()`，不要依赖系统墙上时间计算移动距离。
- App 切换、服务调度延迟或设备休眠后，下一次 tick 应根据实际 elapsed 时间补偿。
- 为防止一次挂起导致瞬间跳跃，可设置单次最大补偿间隔；超过阈值应进入错误或暂停状态，而不是无条件瞬移很长距离。
- 暂停期间不增加距离，恢复时从恢复时刻重新开始计时。

### 8.4 Location 元数据

每次创建虚拟 `Location` 时设置：

- latitude；
- longitude；
- time；
- elapsed realtime nanos；
- speed；
- bearing；
- accuracy 等现有可选字段。

速度和方向应与当前路线状态一致，不要只复用用户设置的固定速度字段。

如果没有有效 bearing：

- 起点可以使用上一方向或原始位置方向；
- 轨迹点足够时使用前后点计算；
- 不要随意填充 0 造成所有方向都指向北方。

---

## 9. 前台服务

### 9.1 服务职责

新增 `WalkingSimulationService`，只负责：

- 读取已准备好的路线；
- 启动和维护步行会话；
- 定时推进位置；
- 更新共享动态位置；
- 更新通知；
- 响应开始、暂停、继续、停止命令。

路线规划请求不要在 Service 内发起；由 ViewModel/RouteClient 完成后再启动 Service。

### 9.2 Manifest 与权限

根据项目 `targetSdk = 36`，执行时必须按当前 Android 文档确认并配置：

- 前台服务基础权限；
- 与位置类型前台服务相关的权限；
- Service 的 `foregroundServiceType`；
- Android 13+ 通知权限；
- 运行时通知授权处理；
- 必要时的电池优化提示。

不要为了绕过系统限制添加不相关权限。

### 9.3 通知

通知应明确显示：

- MockX 正在模拟步行；
- 当前阶段：行走中/已暂停/已到达；
- 已行走距离或百分比；
- 暂停/继续/停止操作（若系统版本支持）；
- 点击通知返回 Manager App。

通知中不得显示完整 API Key、目标 App 私密信息或不必要的精确地址。

### 9.4 服务命令

建议使用显式 Intent Action：

```text
ACTION_START_WALKING
ACTION_PAUSE_WALKING
ACTION_RESUME_WALKING
ACTION_STOP_WALKING
```

命令必须幂等：

- 已经在行走时再次 START 不重复创建多个任务；
- 已暂停时再次 PAUSE 不报错；
- 已停止时再次 STOP 不崩溃；
- Service 重建时读取共享状态并做安全恢复或转为失败状态。

---

## 10. Xposed 侧改造

### 10.1 位置来源

修改 `LocationUtil`，增加动态步行位置读取：

```text
if walking state is valid:
    use walking_current_* / walking_speed / walking_bearing
else:
    use existing fixed location logic
```

动态位置读取必须集中在 `LocationUtil`，不要在每个 Hook 文件中分别读取 Preference。

### 10.2 有效性检查

动态状态只有同时满足以下条件才有效：

- 步行模式已启用；
- phase 是 `WALKING`、`PAUSED` 或 `ARRIVED`；
- 当前经纬度有限且在合法范围；
- `walking_updated_at` 存在且没有严重过期；
- 目标 App/系统 Hook 当前允许伪装。

如果状态过期：

- 记录一次脱敏日志；
- 按项目现有策略回退或停止伪装；
- 不继续返回最后一个无限期旧坐标。

### 10.3 Hook 兼容性

保持以下行为不变：

- 目标 App 包名过滤；
- `getLastLocation`；
- `getCurrentLocation`；
- Provider location dispatch；
- Receiver callback；
- GNSS、地理围栏等相关 Hook；
- Wi-Fi 与基站防泄漏逻辑。

不要在 `system_server` Hook 内启动协程、创建线程、发 HTTP 请求或执行地图路线算法。

### 10.4 真实位置泄漏防护

步行模拟期间继续保持现有保护：

- 目标 App 不能通过 Wi-Fi 扫描结果反查真实位置；
- 目标 App 不能通过基站信息反查真实位置；
- 系统定位分发不能把真实位置直接推给已启用伪装的目标注册；
- 目标 App 只能得到当前动态模拟位置。

---

## 11. UI 实现要求

### 11.1 地图页面

复用当前 `MapScreen`、`MapViewModel` 和地图效果组件，新增：

- 步行规划入口；
- 规划中 Loading；
- 路线 Polyline；
- 路线总距离；
- 预计用时；
- 当前进度；
- 当前速度选择；
- 开始/暂停/继续/停止按钮；
- 错误提示。

### 11.2 状态显示

地图顶部状态至少区分：

```text
未选择目标
已选择目标
正在规划路线
路线已准备
模拟步行中
已暂停
已到达
正在停止
规划失败
```

### 11.3 交互校验

开始前检查：

- 高德 Key 已配置；
- 真实位置已获取；
- 目标点存在；
- 目标点不是明显无效坐标；
- 路线至少有两个轨迹点；
- Xposed/共享偏好服务可用；
- 必要权限和通知条件满足。

以下情况不能开始：

- 只选择了目标但没有真实起点；
- Key 为空；
- 路线 API 返回错误；
- 路线距离为 0；
- 当前已有固定位置会话但未确认替换；
- Service 启动失败。

### 11.4 地图坐标绘制

- 内部路线统一保存 WGS-84；
- 高德图源显示时将路线点转为 GCJ-02；
- OSM/天地图等 WGS-84 图源直接绘制 WGS-84；
- 切换图源时只改变显示坐标，不改变路线真实数据；
- 当前模拟 Marker 同样遵循图源坐标转换规则。

---

## 12. 外部控制广播兼容

现有 `ControlReceiver` 支持：

```text
ACTION_START
ACTION_STOP
ACTION_SET_LOCATION
```

第一版可以只通过 Manager App UI 控制步行，或增加以下广播：

```text
ACTION_START_WALK
ACTION_PAUSE_WALK
ACTION_RESUME_WALK
ACTION_STOP_WALK
```

如果增加广播：

- 继续遵循现有 receiver 默认关闭策略；
- 不通过广播传入高德 Key；
- 对路线和速度参数做范围校验；
- 外部控制失败必须返回日志或可观察状态；
- 不允许任意第三方应用在未启用外部控制时启动步行服务。

---

## 13. 错误处理

建议定义稳定错误类型：

```text
MISSING_API_KEY
LOCATION_UNAVAILABLE
INVALID_DESTINATION
NETWORK_TIMEOUT
NETWORK_UNAVAILABLE
API_AUTH_FAILED
API_QUOTA_EXCEEDED
NO_ROUTE
INVALID_ROUTE_DATA
PREFERENCE_UNAVAILABLE
SERVICE_START_FAILED
LOCATION_STATE_STALE
```

UI 只显示用户可理解的中文消息；日志记录：

- 错误类型；
- HTTP 状态（如有）；
- 高德业务状态码（如有）；
- 请求阶段；
- 不记录 Key；
- 不记录完整请求 URL；
- 不记录不必要的精确位置。

路线失败时：

- 保留用户选择的目的地；
- 清除半成品路线；
- 不开启步行 Service；
- 不把 `is_playing` 错误设置为 true。

Service 异常时：

- 停止自身 tick；
- 将状态设置为 `FAILED`；
- 保留最后安全坐标或按现有策略恢复真实位置；
- 更新通知；
- 允许用户重新规划。

---

## 14. 测试计划

### 14.1 单元测试

新增测试覆盖：

1. 高德响应成功解析。
2. 高德错误响应解析。
3. 空 `paths`。
4. 空 `steps`。
5. 缺少 polyline。
6. 非法经纬度。
7. GCJ-02 → WGS-84 转换。
8. WGS-84 → GCJ-02 绘制转换。
9. 路线点去重。
10. 累计距离计算。
11. 距离插值。
12. 起点、终点边界。
13. 到达状态。
14. 暂停不推进。
15. 继续从暂停点推进。
16. 速度变化。
17. bearing 计算。
18. elapsed realtime 补偿。
19. 过期动态状态回退。
20. 固定位置模式不受影响。

### 14.2 API 手工验证

使用本地设置页输入测试 Key，验证：

- 正常城市内步行路线；
- 起点和终点很近；
- 起点和终点不可步行到达；
- Key 无效；
- Key 未配置；
- 网络断开；
- API 配额错误；
- HTTP 200 但业务状态失败；
- 返回路线点数量较多。

验证完成后删除本机 Key 并在高德控制台重置测试 Key。

### 14.3 设备测试

至少验证：

- Android 9/10 兼容最低版本；
- 当前目标 Android/HyperOS 设备；
- Manager App 前台运行；
- 切换到目标 App；
- 锁屏 1–3 分钟；
- 暂停/继续；
- 到达终点；
- 停止并恢复真实位置；
- 重启 Manager App；
- Service 被系统重建；
- 系统 Hook 开启/关闭；
- 目标 App 过滤开启/关闭。

### 14.4 目标 App 验证

使用至少一种普通定位 App 和一种会自行融合 Wi-Fi/基站定位的地图类 App，检查：

- 坐标随路线变化；
- 坐标没有跳回真实位置；
- 速度字段合理；
- bearing 不固定为 0；
- Wi-Fi/基站不能泄漏真实位置；
- 高德/百度/腾讯类 App 不因网络定位重新定位到真实位置。

编译成功不等于功能成功，必须在已启用模块的实体设备上做运行时验证。

---

## 15. 安全、隐私与合规

- 当前测试 Key 只用于短期本地开发验证。
- 不把 Key 提交到 Git。
- 不把 Key 放入 APK 默认值。
- 不把 Key 写入 issue、README、截图、日志或测试报告。
- 开发完成后必须在高德控制台重置该测试 Key。
- 发布版本应改为用户自带 Key或后端代理模式。
- 应在项目隐私说明中说明路线请求会向高德服务发送起点和终点坐标。
- 只有用户主动开始路线规划时才发送路线坐标。
- 不上传完整路线、目标 App 列表或无关设备信息。
- 遵守高德开放平台服务协议、配额限制和应用合规要求。
- 本功能定位于自有设备上的位置类应用开发与测试，不用于绕过第三方平台规则。

---

## 16. 推荐实施顺序

按以下顺序实施，避免一次改动过多导致定位链路难以排错：

### 阶段 1：基础模型与设置

- 增加高德 Key 设置项。
- 增加坐标、路线、步行状态模型。
- 增加 Preference 读写。
- 补充单元测试。
- 暂不接 Service 和 Xposed 动态状态。

验收：Key 能保存/清除，重启 App 后状态正确，仓库无 Key。

### 阶段 2：路线 API

- 实现高德 Web 路线客户端。
- 实现响应解析。
- 实现 GCJ-02/WGS-84 适配。
- 用固定测试坐标做 API 验证。

验收：能获得至少两个有效 WGS-84 路线点，并能显示距离和预计时间。

### 阶段 3：路线预览 UI

- 接入当前真实位置。
- 地图目标点作为终点。
- 显示路线 Polyline。
- 增加速度选择和规划错误提示。

验收：真实位置到目标点的步行路线能在当前图源上正确贴合道路。

### 阶段 4：前台步行 Service

- 实现路线推进器。
- 实现每秒动态位置更新。
- 实现暂停/继续/停止。
- 实现前台通知。
- 增加服务生命周期测试。

验收：不依赖目标 App 的定位请求，Service 自己持续更新共享动态位置。

### 阶段 5：Xposed 动态位置接入

- `LocationUtil` 增加动态状态优先级。
- 所有现有 Hook 使用统一动态位置。
- 保持固定位置回退。
- 增加状态过期保护。

验收：目标 App 收到的坐标随路线移动，固定位置功能无回归。

### 阶段 6：后台和设备验证

- 切换 App。
- 锁屏。
- Service 重建。
- 目标 App 网络定位防泄漏。
- 多 ROM/多 Android 版本验证。

验收：得到实际设备日志、屏幕操作结果和目标 App 位置变化证据，而不是只依据编译成功。

---

## 17. 最终验收标准

功能只有同时满足以下条件才算完成：

- Key 可以通过设置页配置，源码和仓库中无 Key。
- 未配置 Key 时不会发起路线请求。
- 能从真实位置规划到地图目标的步行路线。
- 路线能正确解析并显示。
- 高德 GCJ-02 与 Android WGS-84 转换正确。
- 模拟位置沿道路轨迹连续移动。
- `Location.speed` 和 `bearing` 与步行状态一致。
- 暂停后位置和进度保持不变。
- 继续后从暂停点继续。
- 到达后停留在终点。
- 停止后按现有逻辑恢复真实位置。
- 切换 App 和锁屏后仍能继续一段时间。
- 目标 App 不会通过 Wi-Fi/基站位置回退到真实地点。
- 固定点模拟、目标包过滤、系统 Hook 和现有地图功能没有回归。
- API 失败、Key 无效、路线为空、服务异常都有清晰可恢复状态。
- 高德测试 Key 已删除/重置，未进入 Git 和发布产物。
